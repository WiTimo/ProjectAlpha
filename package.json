{
  "name": "project-alpha",
  "version": "0.8.0",
  "private": true,
  "description": "Helper npm scripts for running preprocessing and training tasks",
  "scripts": {
    "preprocessing": "cd preprocessing && cargo run --bin preprocessing",
    "parquet": "python preprocessing/test/describe_parquet.py --input data/preprocessed/fast/20240610.parquet --output preprocessing/test/parquet-output.txt && python preprocessing/test/describe_parquet.py --input data/preprocessed/20240610.parquet --output preprocessing/test/parquet-labels-output.txt",
    "packages": "python -m pip install -r training/requirements.txt && python -m pip install -r deployment/requirements.txt",
    "training": "python training/tcn.py",
    "realtime": "cd preprocessing && cargo run --bin realtime -- --resolutions fast,mid,slow --source C:/Ninjatrader/L2Log.txt --emit ../runs/realtime/features.jsonl",
    "deployment": "cd deployment && python realtime_inference.py --model-bundle ../runs/models/best_model.pt --features ../runs/realtime/features.jsonl --resolutions fast mid slow --trade-threshold 0.75 --entry-min-price-move 10.0 --entry-price-feature mid_close_price",
    "evaluation": "cargo build --manifest-path preprocessing/Cargo.toml && python evaluation/realtime_eval.py"
  },
  "engines": {
    "node": ">=14"
  }
}
