{
  "name": "project-alpha",
  "version": "0.1.0",
  "private": true,
  "description": "Helper npm scripts for running preprocessing and training tasks",
  "scripts": {
    "preprocessing:dry": "cd preprocessing && cargo run -- --dry-run",
    "preprocessing": "cd preprocessing && cargo run -- --input ../data/raw --output ../data/preprocessed --resolutions fast,mid,slow",
    "preprocessing:fmt": "cd preprocessing && cargo fmt",
    "preprocessing:build": "cd preprocessing && cargo build --release",
    "preprocessing:clean": "cd preprocessing && cargo clean",
    "preprocessing:investigate": "python preprocessing/test/describe_parquet.py --input data/preprocessed/fast/20231219.parquet --output preprocessing/test/parquet-output.txt",
    "preprocessing:investigate:labels": "python preprocessing/test/describe_parquet.py --input data/preprocessed/20231219.parquet --output preprocessing/test/parquet-labels-output.txt",
    "fresh": "rm -rf data/preprocessed/* && npm run preprocessing",
    "training:install": "python -m pip install -r training/requirements.txt",
    "training:fast": "python training/tcn.py --feature-root data/preprocessed --label-root data/preprocessed --resolution fast --epochs 32 --val-days 5 --test-days 5",
    "training:mid": "python training/tcn.py --feature-root data/preprocessed --label-root data/preprocessed --resolution mid --epochs 32",
    "training:slow": "python training/tcn.py --feature-root data/preprocessed --label-root data/preprocessed --resolution slow --epochs 32",
    "training:multi": "python training/tcn.py --feature-root data/preprocessed --label-root data/preprocessed --resolutions fast mid slow --epochs 32 --val-days 5 --test-days 5",
    "training:multi:regimes": "python training/tcn.py --feature-root data/preprocessed --label-root data/preprocessed --resolutions fast mid slow --enable-regime-training --model-output-dir runs/regimes --epochs 32 --val-days 5 --test-days 5",
    "help": "node -e \"console.log(Object.keys(require('./package.json').scripts).join('\\n'))\""
  },
  "engines": {
    "node": ">=14"
  }
}
